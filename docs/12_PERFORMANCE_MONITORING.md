# Мониторинг производительности и защита от деградации модели

## Обзор

Система мониторинга производительности отслеживает работу стратегии в реальном времени и автоматически реагирует на отклонения от ожидаемых результатов. Это защищает от классической проблемы AI-стратегий: модель работает хорошо на бэктесте и в начале, но затем начинаются серии убытков.

## Что такое дрифт модели?

**Дрифт модели** (Model Drift) — это ситуация, когда распределение входных данных или поведение рынка меняется так, что модель, обученная на старых данных, перестает работать на новых.

### Примеры дрифта:

1. **Изменение волатильности**: Рынок стал более волатильным (или наоборот)
2. **Изменение тренда**: Бычий рынок сменился медвежьим
3. **Новые рыночные условия**: Кризис, важные новости, изменение ликвидности
4. **Изменение корреляций**: Фичи стали вести себя по-другому

### Как это обнаруживается:

Система сравнивает текущее распределение фичей с распределением из обучающей выборки:
- Средние значения (mean)
- Стандартные отклонения (std)
- Если различия превышают порог → обнаружен дрифт

## Компоненты системы

### 1. PerformanceMonitor

Отслеживает метрики производительности:
- Win Rate (скользящее окно)
- Profit Factor
- Серии убытков
- Максимальная просадка
- Средняя уверенность модели

### 2. ModelDriftDetector

Обнаруживает дрифт модели:
- Сравнивает распределение фичей
- Вычисляет оценку дрифта (0-1)
- Отслеживает изменения во времени

### 3. MonitoringVisualizer

Создает визуализации:
- Дашборд с основными метриками
- График дрифта модели
- Equity curve с отметками статусов

## Конфигурация

Все параметры настраиваются в `config/monitoring_config.py`:

```python
from config.monitoring_config import MonitoringConfig

# Создаем кастомную конфигурацию
monitoring_config = MonitoringConfig(
    window_size=50,  # Размер окна для скользящих метрик
    
    # Пороги для предупреждения (WARNING)
    warning_thresholds={
        'win_rate_drop': 0.15,      # Падение Win Rate на 15%
        'profit_factor_drop': 0.30,  # Падение Profit Factor на 30%
        'consecutive_losses': 3,     # 3 убытка подряд
        'max_drawdown': 0.15,        # Просадка > 15%
        'drift_score': 0.05,         # Дрифт > 5%
    },
    
    # Пороги для критической ситуации (CRITICAL)
    critical_thresholds={
        'consecutive_losses': 5,     # 5 убытков подряд
        'max_drawdown': 0.20,        # Просадка > 20%
        'win_rate_drop': 0.30,       # Падение Win Rate на 30%
        'drift_score': 0.10,         # Дрифт > 10%
    },
    
    # Пороги для остановки (STOPPED)
    stopped_thresholds={
        'consecutive_losses': 10,    # 10 убытков подряд
        'max_drawdown': 0.30,        # Просадка > 30%
    },
    
    # Множители размера позиций
    position_size_multipliers={
        'NORMAL': 1.0,    # Полный размер
        'WARNING': 0.5,   # Снижение на 50%
        'CRITICAL': 0.25, # Снижение на 75%
        'STOPPED': 0.0,   # Остановка
    }
)
```

## Использование

### В бэктестинге

```python
from trading.backtester import Backtester
from config.monitoring_config import MonitoringConfig

# Создаем конфигурацию мониторинга
monitoring_config = MonitoringConfig(
    window_size=50,
    warning_thresholds={'consecutive_losses': 3, 'max_drawdown': 0.15},
    critical_thresholds={'consecutive_losses': 5, 'max_drawdown': 0.20}
)

# Создаем бэктестер с мониторингом
backtester = Backtester(
    model_path='workspace/models/checkpoints/encoder_model.pth',
    scaler_path='workspace/prepared/scalers/feature_scaler_encoder.pkl',
    model_type='encoder',
    monitoring_config=monitoring_config,
    enable_monitoring=True  # Включить мониторинг
)

# Запускаем бэктест
results = backtester.backtest(df, save_plots=True)

# Результаты содержат отчет о мониторинге
if 'performance_monitoring' in results:
    monitor_report = results['performance_monitoring']
    print(f"Статус: {monitor_report['status']}")
    print(f"Серия убытков: {monitor_report['consecutive_losses']}")
    print(f"Просадка: {monitor_report['max_drawdown']:.1%}")
```

### В реальной торговле

```python
# После каждого закрытия позиции
backtester.performance_monitor.add_trade(
    profit=trade_profit,
    confidence=signal_confidence,
    timestamp=datetime.now(),
    direction=direction,
    entry_price=entry_price,
    exit_price=exit_price,
    exit_reason='tp'
)

# Проверяем статус
if backtester.performance_monitor.should_stop_trading():
    print("⚠️ Торговля остановлена!")
    # Остановить торговлю

# Получаем множитель размера позиции
multiplier = backtester.performance_monitor.get_position_size_multiplier()
lot_size = base_lot_size * multiplier
```

## Статусы системы

### 🟢 NORMAL
Все метрики в норме, торговля идет с полным размером позиций.

### 🟡 WARNING
Обнаружены отклонения:
- Win Rate упал на 15%
- Profit Factor упал на 30%
- 3 убытка подряд
- Просадка > 15%

**Действия**: Размер позиций снижается на 50%

### 🔴 CRITICAL
Критическая ситуация:
- 5+ убытков подряд
- Просадка > 20%
- Win Rate упал на 30%
- Дрифт модели > 10%

**Действия**: Размер позиций снижается на 75%

### ⛔ STOPPED
Полная остановка:
- 10+ убытков подряд
- Просадка > 30%
- Win Rate упал на 50%

**Действия**: Торговля полностью остановлена

## Визуализация

После бэктеста автоматически создаются графики:

1. **Performance Dashboard** (`trading/monitoring_plots/dashboard_*.png`):
   - Equity curve с отметками статусов
   - Win Rate (текущий vs ожидаемый)
   - Profit Factor
   - Средняя уверенность
   - Просадка
   - Частота аномалий
   - Временная линия статусов

2. **Drift Analysis** (`trading/monitoring_plots/drift_*.png`):
   - График дрифта модели во времени
   - Пороги предупреждения

## Рекомендации по настройке

### Для консервативной стратегии:
```python
warning_thresholds = {
    'consecutive_losses': 2,      # Более чувствительно
    'max_drawdown': 0.10,         # 10% просадка
    'win_rate_drop': 0.10,        # 10% падение
}
```

### Для агрессивной стратегии:
```python
warning_thresholds = {
    'consecutive_losses': 5,      # Менее чувствительно
    'max_drawdown': 0.20,         # 20% просадка
    'win_rate_drop': 0.20,        # 20% падение
}
```

### Для реальной торговли:
1. Начните с консервативных порогов
2. Наблюдайте за поведением системы
3. Постепенно корректируйте пороги на основе опыта
4. Всегда тестируйте изменения на исторических данных

## Интерпретация результатов

### Нормальная работа:
- Статус: NORMAL
- Win Rate близок к ожидаемому
- Profit Factor стабилен
- Дрифт < 5%

### Требуется внимание:
- Статус: WARNING
- Win Rate упал на 10-15%
- Дрифт 5-10%
- **Действие**: Снизить размер позиций, усилить мониторинг

### Критическая ситуация:
- Статус: CRITICAL или STOPPED
- Серия убытков
- Высокий дрифт (>10%)
- **Действие**: Остановить торговлю, проанализировать причины, переобучить модель

## Часто задаваемые вопросы

**Q: Как часто проверяется дрифт?**  
A: По умолчанию каждые 10 сделок (настраивается в `drift_detection['check_interval']`)

**Q: Можно ли отключить мониторинг?**  
A: Да, установите `enable_monitoring=False` при создании Backtester

**Q: Как интерпретировать дрифт 0.15?**  
A: Это означает, что распределение фичей изменилось на 15% относительно обучающей выборки. Порог предупреждения обычно 5-10%.

**Q: Что делать при обнаружении дрифта?**  
A: 
1. Проверить, не изменились ли рыночные условия
2. Убедиться, что данные загружаются корректно
3. Рассмотреть переобучение модели на новых данных
4. Временно снизить размер позиций или остановить торговлю

## Следующие шаги

1. Запустите бэктест с включенным мониторингом
2. Изучите созданные графики
3. Настройте пороги под вашу стратегию
4. Интегрируйте в реальную торговлю

